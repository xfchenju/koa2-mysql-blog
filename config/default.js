const config = {
    // 启动端口
    port: 3000,

    // 数据库配置
    database: {
        DATABASE: 'koa2-blog',
        USERNAME: 'root',
        PASSWORD: '',
        PORT: '3306',
        HOST: 'localhost'
    }
};

module.exports = config;